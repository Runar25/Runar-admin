
# Scratch patch runner (Code rewrites it per task — never commit)
# _patch.py = CODE-tree · _patch_tune.py = CODE-tune (dvě lane, dvě cesty, ať si
# session nepřepisují rozdělaný skript pod rukama)
scripts/_patch.py
scripts/_patch_tune.py

__pycache__/

# Eval export — user reading text + follow-ups. NEVER commit (PII). Local only.
readings_export.json
readings_export.csv

# Synthetic eval batches (scripts/utils/gen_batch.js). Generated, bulky, never
# a source of truth — and must never be mistaken for real readings. Local only.
eval_out/

# Scratch/backup — untracked pracovní plocha, JEDNOZNAČNĚ se necommitují.
# 2026-07-19: `git add -A v2` jich sebral 112 do cizí lane. Gitignore je zastaví
# u zdroje (git add -A je přeskočí). Zálohu proti sweepu jistí i pre-commit hook.
# POZOR: v2/tree-lab-* tu NENÍ — má trackovaný zdroj (runar-branch.js, runar-trunk.js),
# který CODE-tree edituje. Ten kryje jen hook (blokuje nový ADD, pustí modifikaci trackovaného).
v2/tree-snapshots/
_backup/

# 2026-08-09: v2/sigil-lab už trackovaný zdroj NEMÁ (na rozdíl od tree-lab-*). Celá sigil
# lane — builder, lab i doc — se přesunula MIMO repo do C:\Users\zkuku\runar-sigil\, protože
# Runar25/Runar-admin je VEŘEJNÉ a owner rozhodl "interní, je to jen pracovní verze".
# Ignoruje se proto natvrdo, ať ji `git add -A` nemůže vrátit zpátky.
v2/sigil-lab/

# Cowork snapshot kopie kanonu — zdroj driftu, necommitovat (audit 2026-08-02)
_cowork_snap/

# Vygenerovany snimek provozu (stats.js --html). Snimek ke dni, ne zdroj.
runar-provoz.html
